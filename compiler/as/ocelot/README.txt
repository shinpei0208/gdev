To build PTXToSASSTranslator:

1. Type `cmake .' to build the Makefile.

2. Type `make' to compile the libocelot and PTXToSASSTranslator.

3. Type `sudo make install' to install the PTXToSASSTranslator to
   /usr/local/gdev/bin.
